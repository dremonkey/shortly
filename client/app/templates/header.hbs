<ul class="nav nav-pills pull-right">
  {{#each userLinks}}
    <li><a href="{{this.href}}" class="{{this.classes}}">{{this.title}}</a></li>
  {{/each}}
</ul>
<ul class="nav nav-pills pull-right">
  {{#each navLinks}}
    <li><a href="{{this.href}}" class="{{this.classes}}">{{this.title}}</a></li>
  {{/each}}
</ul>
<h3 class="text-muted">{{brand}}</h3>